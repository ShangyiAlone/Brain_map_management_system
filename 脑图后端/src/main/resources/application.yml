server:
  port: 8080

spring:
  datasource:
    druid:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/brain_file?serverTimezone=UTC
      username: root
      password: shangyi

mybatis-plus:
  global-config:
    db-config:
      table-prefix: t_
      id-type: auto

  #设置日志
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

